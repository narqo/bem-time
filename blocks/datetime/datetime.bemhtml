block('datetime')(
    tag()('time'),
    js()(true),
    attrs()(function() {
        var ctx = this.ctx;
        return {
            datetime : ctx.val,
            title : ctx.title
        };
    })
)
